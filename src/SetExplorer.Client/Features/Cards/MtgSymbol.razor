@using SetExplorer.Client.Core.Scryfall.Symbology.Models

<abbr title="@Symbol.Description" class="card-symbol">@Symbol.Symbol</abbr>

@code {
    [Parameter, EditorRequired]
    public required ScryfallMtgSymbol Symbol { get; set; }
}

<style>
    .card-symbol {
        margin-left: 0.1rem;
        background-image: url(@Symbol.SvgUri);
    }
</style>